version: '3.7'

networks:
  network:
    name: net_1
    external: true

services:
    database:
        image: postgres:12
        ports:
            - "5432:5432"
        networks:
            - network
        environment:
            POSTGRES_USER: coursework
            POSTGRES_PASSWORD: coursework
            POSTGRES_DB: coursework
